@model prjShoppingForum.Models.Entity.tUserProfile

<h1>我的積分</h1>
@*TODO:會再修改*@

<table class="table">
    <thead>
        <tr>
            <th>姓名</th>
            <th>積分</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            @if (Model!= null)
            {
                <td>@Model.fName</td>
                <td>@Model.fScore</td>
            }
        </tr>
    </tbody>
</table>

